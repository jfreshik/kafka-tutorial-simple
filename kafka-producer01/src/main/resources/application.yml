server:
  port: 8088

spring:
  kafka:
    bootstrap-servers: 127.0.0.1:9092
    topic: simple-topic

    topic-setting:
      partitions: 3
      replicas: 1

logging.level:
  org:
    apache.kafka: DEBUG
